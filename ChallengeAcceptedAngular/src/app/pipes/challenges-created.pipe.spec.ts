import { ChallengesCreatedPipe } from './challenges-created.pipe';

describe('ChallengesCreatedPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengesCreatedPipe();
    expect(pipe).toBeTruthy();
  });
});
