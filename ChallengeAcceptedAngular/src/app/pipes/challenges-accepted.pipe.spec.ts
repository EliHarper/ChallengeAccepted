import { ChallengesAcceptedPipe } from './challenges-accepted.pipe';

describe('ChallengesAcceptedPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengesAcceptedPipe();
    expect(pipe).toBeTruthy();
  });
});
