import { TopSkillsPipe } from './top-skills.pipe';

describe('TopSkillsPipe', () => {
  it('create an instance', () => {
    const pipe = new TopSkillsPipe();
    expect(pipe).toBeTruthy();
  });
});
